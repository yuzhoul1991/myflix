- @categories.each do |category|
  %article.video_category
    %header
      %h3= link_to category.name
    .videos.row
      - category.recent_videos.each do |video|
        .video.col-sm-2
          = link_to "", nil do
            %img(src="/tmp/#{video.title}.jpg")
